#include "customers.h"
customers* CreateAccount(customers *);
void DisplayDetails(customers* );
void DepositAmount(customers*);
void WithdrawAmount(customers*);
customers* FindingAccount(customers*);
void TransferMoney(customers* );
void historyTransaction(customers*);
void save(customers*);
customers* sync_file(customers*);
